<%- include('../partials/head', { title: 'Error' }) %>
<div style="min-height:100vh; display:flex; align-items:center; justify-content:center; text-align:center; padding:2rem;">
  <div>
    <div style="font-size:5rem; margin-bottom:1rem;">⚠️</div>
    <h1 style="font-size:1.8rem; font-weight:800; margin-bottom:0.5rem;"><%= title || 'Error' %></h1>
    <p style="color:var(--text2); margin-bottom:2rem;"><%= error || 'Something went wrong.' %></p>
    <a href="/servers" class="btn btn-primary"><i class="fa-solid fa-arrow-left"></i> Back to Servers</a>
  </div>
</div>
<%- include('../partials/foot', { flash: null }) %>
